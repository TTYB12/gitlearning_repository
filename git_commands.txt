git init
git add <file>
git status
git commit
git push
git pull
git clone

git config --global user.name 'XXX'
git config --global user.email 'XXX@xxx.com'

git rm --cached <file>  // to undo git add


git add *.html // git add all the files with html extension

touch .gitignore 

git branch <branch name>

git checkout <branch name>

git merge <branch name> // merge a branch to the master

git remote 



ls -ah // to see .git and other invisible files

git diff <file_name> // to see the changes made on the file


